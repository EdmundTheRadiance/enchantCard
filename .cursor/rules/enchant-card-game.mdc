---
description: 附魔牌（Enchant Card）微信小游戏项目约定与当前实现说明，供后续 AI 接手开发
globs: js/**/*.js, game.js
alwaysApply: true
---

# 附魔牌项目规则与当前效果说明

## 项目类型与入口

- **平台**：微信小游戏（Canvas 2D 渲染）
- **入口**：`game.js` 仅做 `import Main from './js/main'; new Main();`
- **画布**：`js/render.js` 中 `wx.createCanvas()`，导出 `SCREEN_WIDTH`、`SCREEN_HEIGHT`；`ctx` 在 `js/main.js` 取得并传入各场景/区域绘制函数

## 模块与文件结构

- **`js/main.js`**：Main 类，状态（scene、startButton、battle、shop）、layout/draw/触摸/各场景入口，不包含具体绘制实现
- **`js/cards/constants.js`**：SUITS、RANKS
- **`js/cards/deck.js`**：createShuffledDeck、drawHandFromDeck、shuffleArray、drawToHandWithReshuffle、moveToDiscardPile、sortHand
- **`js/constants/theme.js`**：COLOR（主文字、强调色、弧线、血量等）、BACKGROUND_GRADIENT（背景渐变配置），供各场景/区域统一改主题
- **`js/utils/draw.js`**：drawGameBackground、roundRect、drawCard、drawCardFanCentered（以牌中心为支点），所有绘制方法首参为 ctx
- **`js/areas/monsterArea.js`**：drawMonsterArea(ctx, w, h, monsterAreaH, battle)
- **`js/areas/scoreArea.js`**：drawScoreArea(ctx, w, h, scoreAreaY, scoreAreaH, battle)
- **`js/areas/handArea.js`**：drawHandArea(ctx, w, h, handAreaY, handAreaH, hand, sortMode, selectedIndices, cardDisplayOffset, discardsRemaining)；getSortButtonRect、getPlayButtonRect、getDiscardButtonRect、getCardPositions、hitTestCard
- **`js/scenes/startScreen.js`**：drawStartScreen(ctx, w, h, startButton)、layoutStartButton(w, h)
- **`js/scenes/battleScene.js`**：drawBattleBackground、drawBattleScene(ctx, w, h, battle)，getRetryButtonRect
- **`js/scenes/shopScene.js`**：drawShopScene(ctx, w, h, shop)，getEnchantButtonRect(w, h)，getShopOptionIndexAt(w, h, options, x, y)，getShopFunctionOptionIndexAt(w, h, functionOptions, x, y)
- **`js/enchant/`**：附魔模块。`constants.js` 效果类型常量；`catalog.js` 功能区附魔选项目录（FUNCTION_ENCHANT_CATALOG：id、label、effect）；`index.js` 导出 getRandomCardTypeOptions(4)、getRandomFunctionOptions(4) 供商店使用

## 场景与状态

- **场景**：`Main.scene` 为 `'start'`、`'battle'` 或 `'shop'`。**调试用**：当前默认 `scene = 'shop'`，一进游戏即商店；正式发布时改回 `scene = 'start'`，由开始界面点击「开始游戏」再进对战。
- **开始界面**：深色渐变背景、标题「附魔牌」、副标题「Enchant Card」、居中「开始游戏」按钮；点击后 `scene = 'battle'`，生成洗好的牌并抽 10 张进 `battle.hand`，然后 `draw()` 重绘
- **对战状态**：`Main.battle` 含 `monsterName`、`monsterHp`、`monsterHpMax`、`chips`、`mult`、`hand`、`drawPile`、`discardPile`、`discardsRemaining`（本局剩余弃牌次数，每局最多 5 次）、`sortMode`、`selectedCardIndices`、`cardDisplayOffset`、`lastHandName`、`lastScore`、`attackAnim`、`failureAnim`、`failed`
- **商店场景**：战胜怪物后进入。`Main.shop` 含 `options`（选牌区随机 4 个选项，来自 `js/enchant`）、`selectedOptionIndex`（选牌区选中 0~3）、`functionOptions`（功能区随机 4 个附魔效果，来自 `js/enchant/catalog`）、`selectedFunctionIndex`（功能区选中 0~3）

## 扑克牌数据

- **花色**：黑桃 ♠、红桃 ♥、方片 ♦、梅花 ♣（用 `\u2660` 等 Unicode，避免引号/编码问题）
- **点数**：`RANKS = ['A','2',...,'10','J','Q','K']`
- **牌堆**：`createShuffledDeck()` 返回 52 张洗好的牌；`drawHandFromDeck(deck)` 从牌堆取前 10 张作为手牌

## 对战界面布局（必须保持比例）

- **竖向三块**（占屏高比例固定）：
  - **怪物区**：0 ~ 50% 屏高。内容：怪物名、怪物形象占位（方形+边框）、血量条
  - **计分区**：50% ~ 65% 屏高。内容：筹码、倍率，放在一个圆角矩形框内
  - **手牌区**：65% ~ 100% 屏高。内容：沿弧排列的手牌 + 下方左侧弃牌按钮（显示剩余次数「弃牌(n)」）、中间排序按钮（按点数/按花色切换）、右侧出牌按钮；`getDiscardButtonRect`、`getSortButtonRect`、`getPlayButtonRect` 供触摸判定，弃牌由 `onDiscardCards()`、出牌由 `onPlayCards()` 处理

## 弃牌规则

- **次数限制**：每局最多弃牌 5 次，`battle.discardsRemaining` 从 5 递减，为 0 时弃牌按钮灰显且点击无效；`resetBattle()` 时重置为 5

## 战胜与商店进入

- **战胜条件**：当出牌造成的分数（筹码×倍率）**大于等于**怪物当前剩余血量时，视为战胜怪物
- **流程**：攻击动画结束后若 `monsterHp <= 0`，调用 `enterShop()`，将 `scene = 'shop'`，并初始化商店选牌区 4 个随机选项；若 `monsterHp > 0` 则进入失败流程（怪物反击→失败界面→再来一次）

## 商店场景布局

- **上下留白**：各占屏高 6%（`TOP_BOTTOM_MARGIN_RATIO`）
- **选牌区**：屏高 40%，自顶部留白下缘起。标题文案为「请选择一种牌进行附魔」；4 个选项按 **2×2** 排列，选项文案分两行展示（上行「随机一张」，下行为点数或花色如 A、黑桃），字号约 18px，选项为按钮样式、与边框留空
- **功能区**：紧接选牌区下方，占屏高 40%。标题「请选择一种附魔效果」；4 个选项来自 `js/enchant/catalog` 随机抽取，2×2 排列、按钮样式、与边框留空，长文案自动换行（约 10 字/行）；`getShopFunctionOptionIndexAt` 供触摸判定
- **附魔按钮**：在功能区下方，占屏高 8% 区域，按钮在该区域内垂直与水平居中；`getEnchantButtonRect(w, h)` 供触摸判定

## 商店选牌区选项规则

- **选项池**：13 种点数（A、2～10、J、Q、K）+ 4 种花色（黑桃、红桃、梅花、方片），共 17 类
- **生成**：进入商店时从 17 类中随机不重复抽取 4 个，作为本局选牌区的 4 个选项；每项为 `{ type: 'rank', rank, label }` 或 `{ type: 'suit', suitIndex, label }`，`label` 如「随机一张A」「随机一张黑桃」。4 个选项在选牌区内以 **2×2** 方式排列，与选牌区边框保留内边距（innerPadding）；选项为**按钮样式**：未选中为灰底+主色描边，选中为强调色底+深色文字+主色描边
- **选择含义**：玩家从 4 个选项中选一个，表示**之后附魔将作用在该类牌上**（如选「随机一张A」即附魔目标为所有 A）；选中状态用 `shop.selectedOptionIndex`（0～3）表示，点击同一选项可取消选中
- **触摸**：`getShopOptionIndexAt(w, h, options, x, y)` 返回选牌区选项下标；`getShopFunctionOptionIndexAt(w, h, functionOptions, x, y)` 返回功能区选项下标
- **功能区选项**：`js/enchant/catalog.js` 的 FUNCTION_ENCHANT_CATALOG 列举所有附魔效果，每项含 id、label、effect、**tier**。进入商店时 `getRandomFunctionOptions(4)` 随机取 4 个；effect 为结构化描述，供后续出牌结算时应用。**附魔等级（tier）**：绿色（green）= 直接倍率/筹码或基于单牌/打出条件的加成（如筹码+5、倍率+1、若这张牌是某花色则+15筹码、若打出牌全红则+3倍率等）；蓝色（blue）= 花色/点数变化凑牌型；紫色（purple）= 条件触发高倍率/筹码；金色（gold）= 条件触发成长型。等级颜色见 `js/enchant/constants.js` 的 ENCHANT_TIER、ENCHANT_TIER_COLOR；商店功能区选项边框按 tier 着色

## 怪物区

- 名字居中顶部；形象为方形占位（可后续替换为图片），尺寸在区内尽量大（约 70% 高、55% 屏宽取小）；血量条在形象下方，显示当前/最大血量，颜色按比例变化

## 计分区

- 一个深色圆角框（`roundRect`），内部分左右：左侧「筹码」+ 数值，右侧「倍率」+ 「×」+ 数值

## 手牌区（核心逻辑，勿随意破坏）

- **弧线**：手牌中心沿圆弧排布，弧弦长为屏宽 60%（`chordWidth = w * 0.6`），半径 `arcRadius = 0.95 * w`；弧线本身不绘制
- **手牌排布**：从弧的起点到终点**均匀**分布 n 张牌（角度 `theta_i = -arcAlpha + (i * 2 * arcAlpha) / (n - 1)`），每张牌的**中心点**在弧线上，旋转角为 `theta`（与弧切线一致）；遮挡自然产生，不单独控制
- **牌尺寸**：固定。`cardH = handAreaH * 0.4`，`cardW = cardH * 0.71`；牌面点数/花色字体略大（rank 20px、suit 16px）
- **牌面绘制**：`drawCardFanCentered` 以牌中心为支点旋转绘制；点数与花色在牌面左上角与右下角展示。手牌抽到后按当前 `sortMode` 排序；按点数时顺序为 A>K>Q>J>10>…>2（大在左）。排序按钮单个，点击切换 sortMode。手牌点击：切换选中状态，选中牌沿弧径向外偏移 10%（牌宽），最多 5 张；偏移量由 `cardDisplayOffset[i]` 与目标（选中=1、未选=0）每帧插值（speed 0.2）实现动画。Main 使用 `requestAnimationFrame` 循环，battle 场景下每帧调用 `updateBattleAnimation` 再 `draw`

## 样式约定

- 背景：深紫/深蓝渐变（如 `#1a0a2e`、`#16213e`、`#0f0f23`）
- 主文字/边框：`#e8d5b7`
- 强调色/按钮：`#c9a227`
- 弧线：`#e83e3e`
- 圆角矩形用 `roundRect` 辅助方法（仅生成 path，不 fill/stroke）

## 出牌与分数

- **牌型**：见 `js/cards/handRank.js`。含德州扑克牌型及五条(120×12)、同花葫芦(140×14)、同花五条(160×16)。牌型从大到小：同花五条、同花葫芦、五条、皇家同花顺、同花顺、四条、葫芦、同花、顺子、三条、两对、一对、高牌。五条/同花五条需 5 张同点数（标准 52 张牌需王牌或后续扩展）。
- **分数**：分数 = 筹码 × 倍率。出牌时用选中牌评估牌型，将 `battle.chips`、`battle.mult` 设为该牌型基础值，`battle.lastScore = chips * mult`，`battle.lastHandName` 用于计分区显示。

## 扩展建议

- 怪物图：将占位矩形改为 `drawImage` 加载资源
- 游戏逻辑：出牌后扣怪物血、补牌、多轮等可基于现有 `battle` 状态扩展
- 商店附魔：`onEnchant()` 根据 `shop.selectedOptionIndex` 与 `shop.options[selectedOptionIndex]` 实现具体附魔效果（如将该类牌标记为已附魔），并决定离开商店后是否回到新一局对战

## 规则维护

- **涉及规则或流程的改动**（如新增场景、修改胜负条件、布局比例、弃牌/选牌/附魔规则等），在实现代码后应**同步更新本文件**（`.cursor/rules/enchant-card-game.mdc`），保证文档与当前行为一致，便于后续协作与 AI 接手。
